---
id: TC-2336
title: "Rich launch tier for Cells"
status: Done
priority: P0
owner: agent_e
updated: "2026-02-18"
spec_ref: "specs/pilots/pilot-aspose-cells-foss-python/run_config.pinned.yaml"
ruleset_version: "v1"
allowed_paths:
  - "specs/pilots/pilot-aspose-cells-foss-python/run_config.pinned.yaml"
  - "plans/taskcards/TC-2336.md"
evidence_required:
  - "launch_tier set to rich"
---

## Objective

Add `launch_tier: "rich"` to the Cells pilot configuration to enable the rich content generation tier, which activates expanded page quotas, additional optional page types, and higher-quality content generation strategies.

## Required spec references

- `specs/pilots/pilot-aspose-cells-foss-python/run_config.pinned.yaml` -- Pilot-specific run configuration
- `specs/schemas/run_config.schema.json` -- Schema defining valid run_config fields
- Plan: `partitioned-shimmying-adleman.md` -- Round 3 Track D configuration

## Scope

### In scope

- Add `launch_tier: "rich"` field to the Cells pilot run_config.pinned.yaml

### Out of scope

- Modifying other pilot configs (3D, Note)
- Implementing launch_tier logic in pipeline code
- Schema validation changes

## Inputs

- Current `specs/pilots/pilot-aspose-cells-foss-python/run_config.pinned.yaml`

## Outputs

- Updated `specs/pilots/pilot-aspose-cells-foss-python/run_config.pinned.yaml` with `launch_tier: "rich"`

## Allowed paths

- `specs/pilots/pilot-aspose-cells-foss-python/run_config.pinned.yaml`
- `plans/taskcards/TC-2336.md`

## Implementation steps

1. Read current run_config.pinned.yaml to understand structure
2. Identify appropriate location for `launch_tier` field (near other top-level config fields like `review_enabled`, `seo_enabled`)
3. Add `launch_tier: "rich"` at the identified location
4. Verify YAML syntax is valid

## Failure modes

1. **YAML syntax error after edit** -- Detection: YAML parse failure or pilot run crash. Resolution: Validate YAML structure after edit. Spec link: run_config.schema.json.
2. **Field placed in wrong YAML nesting level** -- Detection: Pipeline ignores the field because it's nested under a sub-key. Resolution: Place at root level of the YAML document, not indented under any sub-key. Spec link: run_config schema top-level fields.
3. **Typo in field name or value** -- Detection: Pipeline does not recognize launch_tier. Resolution: Use exact string `launch_tier: "rich"` as specified in plan. Spec link: plan Track D TC-2336.

## Task-specific review checklist

- [ ] `launch_tier` field exists at root level of YAML
- [ ] Value is exactly `"rich"` (quoted string)
- [ ] Field is not duplicated
- [ ] No existing fields modified or removed
- [ ] YAML indentation is consistent with rest of file
- [ ] Field placement is logical (near other config flags)

## Deliverables

- Updated `specs/pilots/pilot-aspose-cells-foss-python/run_config.pinned.yaml`
- This taskcard with completion evidence

## Acceptance checks

- [ ] `launch_tier: "rich"` present in run_config.pinned.yaml
- [ ] YAML parses without error
- [ ] No other fields changed

## Self-review

Scoring deferred until implementation complete. Will use 12D template.

## Spec impact

Direct modification of pilot config file. No spec changes required as `launch_tier` is a configuration value consumed by the pipeline at runtime.
