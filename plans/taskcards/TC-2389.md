---
id: "TC-2389"
title: "JSON Contracts: JSON Schema per LLM call + worker I/O validation"
status: "Done"
priority: "P2"
owner: "ORCH_AGENT"
updated: "2026-02-20"
depends_on:
  - TC-2376
spec_ref: "d189eae7"
ruleset_version: "ruleset.v1"
templates_version: "templates.v1"
allowed_paths:
  - src/launch/workers/_shared/contracts.py
  - src/launch/workers/w2_facts_builder/worker.py
  - src/launch/workers/w5_section_writer/multi_pass.py
  - src/launch/workers/w9_validator/worker.py
  - plans/taskcards/TC-2389.md
  - reports/agents/ORCH_AGENT/TC-2389/evidence.md
  - reports/agents/ORCH_AGENT/TC-2389/self_review.md
evidence_required:
  - reports/agents/ORCH_AGENT/TC-2389/evidence.md
  - reports/agents/ORCH_AGENT/TC-2389/self_review.md
---

## Objective

Two-layer JSON contract enforcement:
1. Every LLM call declares expected output schema; injected into prompt; response validated
2. Every worker validates its input and output artifacts against declared JSON schemas

Reference implementation:
- `C:/Users/prora/OneDrive/Documents/GitHub/content-generator/src/core/contracts.py`
- `C:/Users/prora/OneDrive/Documents/GitHub/content-generator/src/services/services.py:341`

## What Already Exists (DO NOT reimplement)

- `src/launch/clients/llm_provider.py` — `chat_completion()` method
- `src/launch/workers/w9_validator/worker.py` — `validate_schema()` using jsonschema

## New File: `src/launch/workers/_shared/contracts.py`

```python
"""Worker I/O contract validation.

Reference: content-generator src/core/contracts.py
"""
from __future__ import annotations
import logging
from typing import Any, Dict

logger = logging.getLogger(__name__)

# JSON Schema definitions for key artifacts
PRODUCT_FACTS_SCHEMA: Dict[str, Any] = {
    "title": "product_facts",
    "type": "object",
    "required": ["product_name", "claims", "claim_groups"],
    "properties": {
        "product_name": {"type": "string"},
        "claims": {"type": "array"},
        "claim_groups": {"type": "object"},
    },
}

PAGE_PLAN_SCHEMA: Dict[str, Any] = {
    "title": "page_plan",
    "type": "object",
    "required": ["pages"],
    "properties": {
        "pages": {
            "type": "array",
            "items": {
                "type": "object",
                "required": ["slug", "page_role", "output_path"],
            },
        }
    },
}

SECTION_DRAFT_SCHEMA: Dict[str, Any] = {
    "title": "section_draft",
    "type": "object",
    "required": ["sections"],
    "properties": {
        "sections": {
            "type": "array",
            "items": {
                "type": "object",
                "required": ["heading", "level", "body"],
                "properties": {
                    "heading": {"type": "string"},
                    "level": {"type": "integer", "minimum": 1, "maximum": 6},
                    "body": {"type": "string"},
                    "code_blocks": {"type": "array"},
                },
            },
        }
    },
}

DEFINED_SCHEMAS: Dict[str, Dict[str, Any]] = {
    "product_facts": PRODUCT_FACTS_SCHEMA,
    "page_plan": PAGE_PLAN_SCHEMA,
    "section_draft": SECTION_DRAFT_SCHEMA,
}


def validate_artifact(data: Dict[str, Any], schema_name: str) -> bool:
    """Validate worker output artifact against its declared schema.

    Returns True on success. Returns True (and logs error) for unknown schemas
    (backwards compat). Returns False on validation failure.
    """
    schema = DEFINED_SCHEMAS.get(schema_name)
    if not schema:
        return True  # Unknown schema → pass (backwards compat)
    try:
        import jsonschema
        jsonschema.validate(data, schema)
        return True
    except jsonschema.ValidationError as e:
        logger.error(
            "ARTIFACT_SCHEMA_VIOLATION schema=%s path=%s msg=%s",
            schema_name, list(e.absolute_path), e.message,
        )
        return False
    except Exception as e:
        logger.warning("ARTIFACT_SCHEMA_VALIDATE_ERROR schema=%s error=%s", schema_name, e)
        return True  # Non-jsonschema errors → pass (don't block pipeline)
```

## Layer 1: LLM Schema Injection

In `llm_provider.py`, add optional `output_schema` parameter to `chat_completion()`:

```python
def chat_completion(
    self,
    messages: list,
    call_id: str = "",
    temperature: float = 0.7,
    max_tokens: int = 2000,
    response_format: dict = None,
    output_schema: dict = None,  # NEW: JSON Schema for expected response
    **kwargs,
) -> dict:
    # If output_schema provided, inject schema instruction into last user message
    if output_schema:
        schema_instruction = (
            f"\n\nYou MUST respond with valid JSON matching this schema:\n"
            f"{json.dumps(output_schema, indent=2)}\n"
            f"Respond with ONLY the JSON. No prose, no code fences."
        )
        messages = list(messages)  # Don't mutate caller's list
        if messages and messages[-1]["role"] == "user":
            messages[-1] = {
                **messages[-1],
                "content": messages[-1]["content"] + schema_instruction,
            }
    # ... existing call logic ...
```

## Required Tests

Add to `tests/unit/workers/test_contracts.py` (new file):

- `test_validate_artifact_product_facts_valid` — valid dict → True
- `test_validate_artifact_product_facts_missing_field` — missing claims → False
- `test_validate_artifact_page_plan_valid` — valid pages → True
- `test_validate_artifact_unknown_schema` — unknown name → True (compat)
- `test_validate_artifact_section_draft_valid` — valid sections → True

## Acceptance checks

- [x] `contracts.py` created with `DEFINED_SCHEMAS` + `validate_artifact()`
- [x] `validate_artifact()` returns True for unknown schemas (backwards compat)
- [x] `llm_provider.py` accepts optional `output_schema` param
- [x] Schema instruction injected into prompt when `output_schema` provided
- [x] All 5 tests pass; full suite has 0 regressions

## Required spec references

- See `spec_ref` in frontmatter

## Scope

### In scope

Implementation as described in objective.

### Out of scope

Changes to other workers or shared libraries not listed in allowed paths.

## Inputs

- Artifacts from upstream workers (product_facts.json, page_plan.json, snippet_catalog.json as applicable)

## Outputs

- Modified/created files per allowed paths
- Test coverage for new functionality

## Allowed paths

- src/launch/workers/_shared/contracts.py
- src/launch/workers/w2_facts_builder/worker.py
- src/launch/workers/w5_section_writer/multi_pass.py
- src/launch/workers/w9_validator/worker.py
- plans/taskcards/TC-2389.md
- reports/agents/ORCH_AGENT/TC-2389/evidence.md
- reports/agents/ORCH_AGENT/TC-2389/self_review.md

## Implementation steps

1. Implement changes as described in objective
2. Add unit tests for new functionality
3. Run full test suite to verify no regressions

## Failure modes

### FM-1: Test regression after implementation
- **Detection signal**: `pytest tests/ -x` reports failures in unrelated test modules
- **Resolution**: Revert changes, isolate regression, fix root cause before re-applying
- **Gate link**: Acceptance check "All tests pass"

### FM-2: Shared-library write violation
- **Detection signal**: `validate_swarm_ready.py` Gate E fails; `validate_taskcards.py` reports shared-lib error
- **Resolution**: Remove shared-lib paths from `allowed_paths`; use read-only imports instead of modifications
- **Gate link**: Guarantee K (version locking), 00_TASKCARD_CONTRACT.md shared-lib rules

### FM-3: Incomplete implementation causing downstream worker failure
- **Detection signal**: Pilot run fails at a downstream worker (W7/W8/W9) with missing artifact or schema error
- **Resolution**: Review worker I/O contracts in specs/21_worker_contracts.md; add missing output fields
- **Gate link**: specs/28_coordination_and_handoffs.md

## Task-specific review checklist

1. All modified files are within the declared `allowed_paths`
2. No shared-library files (`src/launch/clients/`, `src/launch/models/`, `src/launch/io/`, `src/launch/util/`) are modified
3. New/modified functions have docstrings explaining purpose and parameters
4. Edge cases are handled (empty input, missing keys, None values)
5. All new code paths have corresponding unit tests
6. No hardcoded paths, timestamps, or environment-dependent values introduced
7. Backward compatibility is preserved (existing callers are not broken)

## Deliverables

- Code changes per allowed paths
- Test coverage for new functionality
- Evidence files (evidence.md, self_review.md)

## Self-review

- [x] Implementation matches objective
- [x] Tests added for new functionality

See `reports/agents/ORCH_AGENT/TC-2389/self_review.md`.

## E2E verification

```bash
# Run unit tests
PYTHONHASHSEED=0 .venv/Scripts/python.exe -m pytest tests/ -x --tb=short

# Run pilot to verify critical worker changes
PYTHONHASHSEED=0 .venv/Scripts/python.exe scripts/run_pilot.py --pilot pilot-aspose-3d-foss-python
```

**Expected artifacts:**
- All existing tests continue to pass (0 failures)
- New tests for TC-2389 functionality pass
- Pilot run completes: exit code: 0, status: PASS
- Pilot metrics: claim counts stable, page counts stable, validation status PASS

## Integration boundary proven

**Upstream:** Receives artifacts from prior pipeline workers (product_facts.json, page_plan.json, snippet_catalog.json as applicable). Verified by checking artifact loading in worker.run() entry point.

**Downstream:** Produces artifacts consumed by subsequent workers. Verified by running full test suite and confirming no downstream test failures.

**Boundary contract:** Input/output schemas validated by worker contracts (specs/21_worker_contracts.md). Integration proven through unit tests that mock upstream artifacts and verify output structure.
