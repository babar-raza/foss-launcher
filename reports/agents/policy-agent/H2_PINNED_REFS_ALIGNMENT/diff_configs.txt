warning: in the working copy of 'specs/pilots/pilot-aspose-3d-foss-python/run_config.pinned.yaml', LF will be replaced by CRLF the next time Git touches it
warning: in the working copy of 'specs/pilots/pilot-aspose-note-foss-python/run_config.pinned.yaml', LF will be replaced by CRLF the next time Git touches it
diff --git a/specs/pilots/pilot-aspose-3d-foss-python/run_config.pinned.yaml b/specs/pilots/pilot-aspose-3d-foss-python/run_config.pinned.yaml
index 89bfae0..408a016 100644
--- a/specs/pilots/pilot-aspose-3d-foss-python/run_config.pinned.yaml
+++ b/specs/pilots/pilot-aspose-3d-foss-python/run_config.pinned.yaml
@@ -18,7 +18,7 @@ site_ref: "PIN_TO_COMMIT_SHA"
 
 # Optional but recommended for deterministic gate runners.
 workflows_repo_url: "https://github.com/Aspose/aspose.org-workflows"
-workflows_ref: "default_branch"
+workflows_ref: "PIN_TO_COMMIT_SHA"
 
 required_sections: ["products", "docs", "reference", "kb", "blog"]
 
@@ -87,3 +87,14 @@ templates_version: "templates.v1"
 ruleset_version: "ruleset.v1"
 allow_inference: false
 max_fix_attempts: 3
+
+# Budget + Circuit Breakers (Guarantees F & G)
+# These limits enforce resource consumption caps and prevent runaway operations.
+budgets:
+  max_runtime_s: 3600              # 1 hour max wall-clock time
+  max_llm_calls: 500               # Prevent runaway API costs
+  max_llm_tokens: 1000000          # ~1M tokens total (input + output)
+  max_file_writes: 100             # Max files to modify in site repo
+  max_patch_attempts: 5            # Max retry attempts for patch bundles
+  max_lines_per_file: 500          # Prevent mass file rewrites (Guarantee G)
+  max_files_changed: 100           # Total file churn limit (Guarantee G)
diff --git a/specs/pilots/pilot-aspose-note-foss-python/run_config.pinned.yaml b/specs/pilots/pilot-aspose-note-foss-python/run_config.pinned.yaml
index a3bd018..eed1430 100644
--- a/specs/pilots/pilot-aspose-note-foss-python/run_config.pinned.yaml
+++ b/specs/pilots/pilot-aspose-note-foss-python/run_config.pinned.yaml
@@ -18,7 +18,7 @@ site_ref: "PIN_TO_COMMIT_SHA"
 
 # Optional but recommended for deterministic gate runners.
 workflows_repo_url: "https://github.com/Aspose/aspose.org-workflows"
-workflows_ref: "default_branch"
+workflows_ref: "PIN_TO_COMMIT_SHA"
 
 required_sections: ["products", "docs", "reference", "kb", "blog"]
 
@@ -87,3 +87,14 @@ templates_version: "templates.v1"
 ruleset_version: "ruleset.v1"
 allow_inference: false
 max_fix_attempts: 3
+
+# Budget + Circuit Breakers (Guarantees F & G)
+# These limits enforce resource consumption caps and prevent runaway operations.
+budgets:
+  max_runtime_s: 3600              # 1 hour max wall-clock time
+  max_llm_calls: 500               # Prevent runaway API costs
+  max_llm_tokens: 1000000          # ~1M tokens total (input + output)
+  max_file_writes: 100             # Max files to modify in site repo
+  max_patch_attempts: 5            # Max retry attempts for patch bundles
+  max_lines_per_file: 500          # Prevent mass file rewrites (Guarantee G)
+  max_files_changed: 100           # Total file churn limit (Guarantee G)
