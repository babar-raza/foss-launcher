# Blockers Index — <TS>

Rule: if ambiguity requires guessing, STOP that taskcard and file a blocker issue JSON that validates against:
- `specs/schemas/issue.schema.json`

## Known global blockers
- OQ-BATCH-001 — Batch execution semantics (`OPEN_QUESTIONS.md`) — status: KNOWN_BLOCKER

## Taskcard-specific blockers
<!-- Append entries like this:
- <TC-ID> — <title> — filed: reports/agents/<agent_id>/<TC-ID>/blockers/<file>.issue.json — status: OPEN
-->
