# Run Configuration Reference

**Canonical Source**: This is the authoritative reference for `run_config.yaml` schema and options.

**Binding Contract**: The schema is defined in [`specs/schemas/run_config.schema.json`](../schemas/run_config.schema.json).

---

## Table of Contents

1. [Overview](#overview)
2. [Required Fields](#required-fields)
3. [Configuration Reference](#configuration-reference)
4. [Examples](#examples)
5. [Validation Profiles](#validation-profiles)
6. [LLM Disk Cache](#llm-disk-cache)

---

## Overview

The `run_config.yaml` file defines all parameters for a documentation generation run. It specifies:

- **Product identity**: slug, name, family, target platform
- **Source repos**: GitHub repo URL + refs (pinned SHAs for determinism)
- **Site layout**: Content structure, subdomain roots, localization
- **Required sections**: products, docs, reference, kb, blog
- **LLM configuration**: API endpoint, model, decoding parameters
- **MCP configuration**: Server settings for Claude Desktop integration
- **Telemetry**: Endpoint URL, project, auth token
- **Commit service**: GitHub commit service endpoint and templates
- **Budgets**: Runtime limits and circuit breakers

---

## Required Fields

| Field | Type | Description |
|-------|------|-------------|
| `schema_version` | string | Schema version (e.g., "1.0.0") |
| `product_slug` | string | Unique product identifier (e.g., "cells") |
| `product_name` | string | Human-readable product name (e.g., "Aspose.Cells for Python") |
| `family` | string | Product family (e.g., "3d", "cells", "words") |
| `github_repo_url` | string | Product repository URL |
| `github_ref` | string | Commit SHA (40-char hex) for product repo |
| `required_sections` | array | Required content sections |
| `site_layout` | object | Site layout configuration |
| `allowed_paths` | array | Write fence - allowed content paths |
| `llm` | object | LLM provider configuration |
| `mcp` | object | MCP server configuration |
| `telemetry` | object | Telemetry endpoint configuration |
| `commit_service` | object | GitHub commit service configuration |
| `templates_version` | string | Template version lock |
| `ruleset_version` | string | Ruleset version lock |
| `allow_inference` | boolean | Allow LLM inference |
| `max_fix_attempts` | integer | Maximum fix loop attempts |
| `budgets` | object | Runtime budget limits |
| `target_platform` | string | Target platform (python, typescript, etc.) |
| `skip_sections` | array | Sections to skip during page planning |
| `allow_manual_edits` | boolean | Emergency-only manual content edits |
| `seo_enabled` | boolean | Enable W10 SEO Optimizer |
| `taskcard_id` | string | Taskcard ID authorizing file modifications |
| `ingestion` | object | Configurable ingestion settings |

---

## Configuration Reference

### `target_platform`

| Value | Description |
|-------|-------------|
| `python` | Python SDK |
| `typescript` | TypeScript SDK |
| `javascript` | JavaScript SDK |
| `java` | Java SDK |
| `dotnet` | .NET SDK |
| `cpp` | C++ SDK |
| `go` | Go SDK |
| `ruby` | Ruby SDK |
| `php` | PHP SDK |
| `kotlin` | Kotlin SDK |
| `swift` | Swift SDK |
| `rust` | Rust SDK |

### `required_sections`

| Value | Description |
|-------|-------------|
| `products` | Product landing pages |
| `docs` | Documentation and tutorials |
| `reference` | API reference |
| `kb` | Knowledge base (FAQ, troubleshooting) |
| `blog` | Blog posts and announcements |

### `site_layout`

```yaml
site_layout:
  content_root: "content"
  subdomain_roots:
    products: "content/products.aspose.org"
    docs: "content/docs.aspose.org"
    kb: "content/kb.aspose.org"
    reference: "content/reference.aspose.org"
    blog: "content/blog.aspose.org"
  localization:
    mode_by_section:
      products: "dir"
      docs: "dir"
      kb: "dir"
      reference: "dir"
      blog: "filename"
```

### `llm`

```yaml
llm:
  api_base_url: "http://localhost:11434"
  model: "claude-sonnet-4.5"
  decoding:
    temperature: 0.0  # Deterministic: always 0.0
    top_p: 1.0
    max_tokens: 4096
  request_timeout_s: 120
  max_concurrency: 4
  fallback:  # Optional
    api_base_url: "https://api.openai.com/v1"
    model: "gpt-4o"
    request_timeout_s: 300
```

### `mcp`

```yaml
mcp:
  enabled: true
  listen_host: "127.0.0.1"
  listen_port: 8787
  auth_token_env: "MCP_AUTH_TOKEN"  # Optional
```

### `telemetry`

```yaml
telemetry:
  endpoint_url: "http://localhost:8765"
  project: "foss-launcher"
  run_tags: ["production", "pilot"]
  auth_token_env: "TELEMETRY_TOKEN"  # Optional
```

### `commit_service`

```yaml
commit_service:
  endpoint_url: "https://commit-service.aspose.org"
  github_token_env: "GITHUB_TOKEN"
  commit_message_template: "docs({section}): {product} - {count} pages"
  commit_body_template: "Generated by FOSS Launcher\n\n{pages_summary}"
  author_name: "FOSS Launcher Bot"
  author_email: "bot@aspose.org"
```

### `budgets`

```yaml
budgets:
  max_runtime_s: 3600  # 1 hour
  max_llm_calls: 1000
  max_llm_tokens: 500000
  max_file_writes: 500
  max_patch_attempts: 5
  max_lines_per_file: 500
  max_files_changed: 100
```

---

## Examples

### Minimal Pilot Config

```yaml
schema_version: "1.0.0"
product_slug: "pilot-aspose-note-python"
product_name: "Aspose.Note for Python"
family: "note"
target_platform: "python"
github_repo_url: "https://github.com/Aspose/aspose-note-python"
github_ref: "a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2"
required_sections:
  - docs
  - reference
site_layout:
  content_root: "content"
  subdomain_roots:
    docs: "content/docs.aspose.org"
    reference: "content/reference.aspose.org"
allowed_paths:
  - "content/docs.aspose.org/note/python/"
  - "content/reference.aspose.org/note/python/"
llm:
  api_base_url: "http://localhost:11434"
  model: "claude-sonnet-4.5"
  decoding:
    temperature: 0.0
mcp:
  enabled: true
  listen_host: "127.0.0.1"
  listen_port: 8787
telemetry:
  endpoint_url: "http://localhost:8765"
  project: "foss-launcher"
commit_service:
  endpoint_url: "https://commit-service.aspose.org"
  github_token_env: "GITHUB_TOKEN"
  commit_message_template: "docs({section}): {product} - {count} pages"
  commit_body_template: "Generated by FOSS Launcher"
templates_version: "2026-02-01"
ruleset_version: "2026-02-01"
allow_inference: true
max_fix_attempts: 3
budgets:
  max_runtime_s: 3600
  max_llm_calls: 500
  max_llm_tokens: 250000
  max_file_writes: 200
  max_patch_attempts: 3
  max_lines_per_file: 500
  max_files_changed: 50
```

### Full Product Config

```yaml
schema_version: "1.0.0"
product_slug: "cells-python"
product_name: "Aspose.Cells for Python"
family: "cells"
target_platform: "python"
locale: "en"
locales:
  - "en"
  - "ja"
github_repo_url: "https://github.com/Aspose/aspose-cells-python"
github_ref: "a1b2c3d4e5f6a1b2c3d4e5f6a1b2c3d4e5f6a1b2"
site_repo_url: "https://github.com/Aspose/aspose.org"
site_ref: "f1e2d3c4b5a6f1e2d3c4b5a6f1e2d3c4b5a6f1e2"
workflows_repo_url: "https://github.com/Aspose/aspose.org-workflows"
workflows_ref: "g1h2i3j4k5l6g1h2i3j4k5l6g1h2i3j4k5l6g1h2"
required_sections:
  - products
  - docs
  - reference
  - kb
  - blog
skip_sections:
  - blog
site_layout:
  content_root: "content"
  subdomain_roots:
    products: "content/products.aspose.org"
    docs: "content/docs.aspose.org"
    kb: "content/kb.aspose.org"
    reference: "content/reference.aspose.org"
    blog: "content/blog.aspose.org"
  localization:
    mode_by_section:
      products: "dir"
      docs: "dir"
      kb: "dir"
      reference: "dir"
      blog: "filename"
allowed_paths:
  - "content/products.aspose.org/cells/python/"
  - "content/docs.aspose.org/cells/python/"
  - "content/reference.aspose.org/cells/python/"
  - "content/kb.aspose.org/cells/python/"
canonical_urls:
  product: "https://products.aspose.com/cells/python/"
  docs: "https://docs.aspose.com/cells/python/"
  reference: "https://reference.aspose.com/cells/python/"
llm:
  api_base_url: "http://localhost:11434"
  model: "claude-sonnet-4.5"
  request_timeout_s: 120
  max_concurrency: 4
  decoding:
    temperature: 0.0
    top_p: 1.0
    max_tokens: 4096
  fallback:
    api_base_url: "https://api.openai.com/v1"
    model: "gpt-4o"
    request_timeout_s: 300
mcp:
  enabled: true
  listen_host: "127.0.0.1"
  listen_port: 8787
  auth_token_env: "MCP_AUTH_TOKEN"
telemetry:
  endpoint_url: "http://localhost:8765"
  project: "foss-launcher"
  run_tags: ["production"]
  auth_token_env: "TELEMETRY_TOKEN"
commit_service:
  endpoint_url: "https://commit-service.aspose.org"
  github_token_env: "GITHUB_TOKEN"
  commit_message_template: "docs({section}): {product} - {count} pages"
  commit_body_template: "Generated by FOSS Launcher\n\n{pages_summary}"
  author_name: "FOSS Launcher Bot"
  author_email: "bot@aspose.org"
templates_version: "2026-02-01"
ruleset_version: "2026-02-01"
allow_inference: true
max_fix_attempts: 3
validation_profile: "prod"
ci_strictness: "strict"
product_type: "sdk"
launch_tier: "rich"
review_enabled: true
seo_enabled: true
taskcard_id: "TC-100"
ingestion:
  scan_directories:
    - "."
  exclude_patterns:
    - "tests/**"
    - "examples/**"
  gitignore_mode: "respect"
  example_directories:
    - "examples"
    - "samples"
  record_binary_files: true
  detect_phantom_paths: true
budgets:
  max_runtime_s: 7200
  max_llm_calls: 2000
  max_llm_tokens: 1000000
  max_file_writes: 1000
  max_patch_attempts: 5
  max_lines_per_file: 1000
  max_files_changed: 200
```

---

## Validation Profiles

| Profile | Use Case | Gate Strictness | Timeouts |
|---------|----------|-----------------|----------|
| `local` | Developer feedback | Skip external links, warnings don't fail | Relaxed (e.g., Hugo build: 300s) |
| `ci` | Continuous integration | All gates including external links | Moderate (e.g., Hugo build: 600s) |
| `prod` | Production PR | Maximum rigor, zero tolerance | Longest (e.g., 600s+) |

See [`specs/09_validation_gates.md`](../specs/09_validation_gates.md) for full gate specifications.

---

---

## LLM Disk Cache

The disk cache speeds up reruns, `launch resume` restarts, and iterative
gate-fix cycles by serving prior LLM responses from disk for identical
requests — without making any network calls.

**The cache is opt-in and disabled by default. No behaviour changes when it is
not explicitly enabled.**

### Enable / disable

```bash
# Enable (must be set before launching the pipeline or pilot script)
export FOSS_LAUNCHER_LLM_CACHE=1

# Disable (default — also achieved by unsetting the variable)
unset FOSS_LAUNCHER_LLM_CACHE
```

### Cache directory

| Priority | Location |
|----------|----------|
| 1 | `FOSS_LAUNCHER_LLM_CACHE_DIR` env var (explicit override) |
| 2 | `<run_dir>/cache/llm/` (default when a run directory exists) |
| 3 | `.cache/llm/` relative to cwd (fallback with no run context) |

```bash
# Override to a shared directory across multiple runs
export FOSS_LAUNCHER_LLM_CACHE_DIR=/tmp/foss-llm-cache
```

### Non-deterministic sampling (temperature > 0)

By default, responses to requests with `temperature > 0` are **not** cached
because sampling results are non-deterministic.  Set the flag below to
override this guard and cache all responses regardless of temperature.

```bash
export FOSS_LAUNCHER_LLM_CACHE_ALLOW_NONDET=1
```

### Fallback-endpoint results

Fallback-endpoint responses (when the primary LLM endpoint fails over) are
**not** saved to cache by default.  Enable with:

```bash
export FOSS_LAUNCHER_LLM_CACHE_FALLBACK=1
```

### Cache key

The cache key is the SHA-256 of the full canonical request payload, covering:
`model`, `messages` (including any schema-injection), `temperature`,
`max_tokens`, `response_format`, and `tools`.  Any change to any of these
fields produces a different key and a fresh network call.

### Implementation notes

- One JSON file per key: `<cache_dir>/<sha256>.json`
- Writes are atomic (temp-file + `os.replace`) — safe under concurrent writes.
- Corrupted or partial cache files are silently treated as misses.
- Cache hits are logged at `DEBUG` level:
  `llm_cache_hit key_prefix=<8chars> call_id=<id>`
- The result dict returned on a cache hit includes `"cache_hit": true` and a
  fresh `"latency_ms"` reflecting the disk-read time (typically < 5 ms).

### Diagnosing cache behavior

Enable `DEBUG` logging to see structured cache events:

```
PYTHONHASHSEED=0 .venv/Scripts/python.exe scripts/run_pilot.py ... 2>&1 | grep llm_cache
```

Each log line includes `outcome`, `reason`, `key_prefix`, and `call_id`:

| Outcome | Reason | Meaning |
|---------|--------|---------|
| `hit` | `ok` | Served from disk; network skipped |
| `miss` | `not_found` | No cache entry yet; network call follows |
| `miss` | `corrupt` | Cache file exists but unreadable; treated as miss |
| `bypass` | `nondet` | `temperature > 0`; set `ALLOW_NONDET=1` to cache |
| `bypass` | `fallback` | Fallback endpoint used; set `CACHE_FALLBACK=1` to cache |
| `saved` | `ok` | Response written to disk cache |

In-process hit/miss/bypass/saved counters are accessible via
`launch.workers._shared.cache_telemetry.get_cache_stats()` for testing and
introspection.

**Cache maintenance** (stats / purge):

```bash
# Print statistics
python scripts/llm_cache_maintenance.py stats

# Delete all cache entries (preview first)
python scripts/llm_cache_maintenance.py purge --dry-run
python scripts/llm_cache_maintenance.py purge

# Delete entries older than 7 days
python scripts/llm_cache_maintenance.py purge-old --days 7
```

**Shared cache across pilot runs** (avoids re-fetching identical responses):

```bash
export FOSS_LAUNCHER_LLM_CACHE_DIR=/tmp/foss-llm-cache-shared
```

---

## See Also

- [`specs/schemas/run_config.schema.json`](../schemas/run_config.schema.json) - JSON Schema (binding)
- [`specs/02_repo_ingestion.md`](../specs/02_repo_ingestion.md) - Ingestion configuration
- [`specs/09_validation_gates.md`](../specs/09_validation_gates.md) - Validation gates
- [`specs/19_toolchain_and_ci.md`](../specs/19_toolchain_and_ci.md) - CI integration
- [`docs/reference/cli.md`](./cli.md) - CLI usage
