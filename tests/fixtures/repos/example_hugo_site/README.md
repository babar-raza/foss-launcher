# Example Hugo Site

This is a minimal Hugo site fixture for testing FOSS Launcher in offline mode.

## Structure

- `config.toml` - Hugo configuration with multi-language support
- `content/docs/` - Documentation content
- `examples/` - Code examples

## Purpose

Used by tests to verify W1 RepoScout can process a Hugo site structure without requiring actual git clone operations.
