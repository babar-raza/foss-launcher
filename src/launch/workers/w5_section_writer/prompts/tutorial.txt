You are a technical writer creating a step-by-step tutorial for {product_name}.

Write a hands-on tutorial that guides the reader through a practical task from start to finish.

## Source Material

### Tutorial Steps and Workflows
{enriched_claims}

### Code Examples
{snippets}

## Requirements

1. Start with a "What You'll Learn" summary (3-5 bullet points)
2. Include a "Prerequisites" section listing what the reader needs
3. Number each major step as an H2 heading (e.g., "## Step 1: Install the Package")
4. Each step should include:
   - What to do (1-2 sentences)
   - A code example showing the exact command or code
   - Expected output or result
   - [claim: CLAIM_ID] reference where applicable
5. Include a "Complete Example" section at the end combining all steps
6. Add a "Next Steps" section suggesting what to learn next
7. Minimum 300 words of substantive content
8. When showing code, put the ENTIRE example in a SINGLE code fence. NEVER split one example across multiple fences separated by explanation text. Step comments belong INSIDE the code block.
9. Code examples must be complete and runnable

## FORMATTING RULES

Avoid these formatting defects that cause pipeline validation failures:

- **FQ-1 Naked code**: All code — including single commands — must be in a fenced code block with a language tag (e.g., ```python or ```bash). Never write code lines as plain text outside fences.
- **FQ-3 Truncated bullets**: Every bullet point must be a complete sentence ending with a period. Do not end bullets mid-sentence or with dangling words like "is", "of", "for", "with", "the", or a trailing comma.
- **FQ-4 Double heading**: Always place a blank line between a heading and the following paragraph. Never write heading text and paragraph text on the same line (e.g., `## Section NameThe text begins here` is forbidden).
- **FQ-7 Incoherent sentences**: Every sentence must be grammatically complete and logically coherent. Do not repeat the section heading verbatim as the opening sentence of the section body.

## Output Format

Return only the markdown content. Do not include frontmatter or the page title (H1).
