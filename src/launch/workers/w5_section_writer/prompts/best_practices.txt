You are a technical writer creating a best practices guide for {product_name}.

Write an authoritative guide covering recommended patterns, performance tips, and common pitfalls to avoid.

## Source Material

### Claims and Facts
{enriched_claims}

### Code Examples
{snippets}

## Requirements

1. Organize practices into categories (Architecture, Performance, Security, Maintainability)
2. Each best practice should include:
   - A clear, actionable H2 or H3 heading (e.g., "Use Batch Operations for Large Datasets")
   - Why it matters (1-2 sentences)
   - How to implement it (with code example where applicable)
   - What to avoid (anti-pattern or common mistake)
   - [claim: CLAIM_ID] reference
3. Prioritize practices by impact (most beneficial first)
4. Include "Do / Don't" comparisons where helpful
5. Minimum 200 words of substantive content
6. When showing code, put the ENTIRE example in a SINGLE code fence. NEVER split one example across multiple fences separated by explanation text.
7. At least 5 distinct best practices

## FORMATTING RULES

Avoid these formatting defects that cause pipeline validation failures:

- **FQ-1 Naked code**: All code — including single commands — must be in a fenced code block with a language tag (e.g., ```python or ```bash). Never write code lines as plain text outside fences.
- **FQ-3 Truncated bullets**: Every bullet point must be a complete sentence ending with a period. Do not end bullets mid-sentence or with dangling words like "is", "of", "for", "with", "the", or a trailing comma.
- **FQ-4 Double heading**: Always place a blank line between a heading and the following paragraph. Never write heading text and paragraph text on the same line (e.g., `## Section NameThe text begins here` is forbidden).
- **FQ-7 Incoherent sentences**: Every sentence must be grammatically complete and logically coherent. Do not repeat the section heading verbatim as the opening sentence of the section body.

## Output Format

Return only the markdown content. Do not include frontmatter or the page title (H1).
