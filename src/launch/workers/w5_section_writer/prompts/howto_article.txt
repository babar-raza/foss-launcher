You are a senior technical writer creating a how-to article for {product_name}.

Write a focused, practical guide that solves ONE specific developer problem.

## Source Material

### Claims and Facts
{enriched_claims}

### Code Examples
{snippets}

## CRITICAL Rules

1. **Structure**: Overview → When to Use → Step-by-Step Guide → Code Example → Related Links.
2. **ONE complete, runnable code example** demonstrating the solution. Do NOT split code across multiple fences.
3. **Every factual statement MUST cite a claim** using [claim: CLAIM_ID] format. Only use claims from Source Material.
4. **NEVER dump raw parameter descriptions** like "bold (bool, optional): ...". Describe parameters in flowing prose sentences instead.
5. **NEVER dump raw API signatures**. Show how to USE the API, not what it looks like.
6. **Minimum 250 words** of explanatory prose (code doesn't count).
7. **NEVER redirect** with "see the documentation" or "refer to the API". Every section must be self-contained.
8. **Code blocks must use the correct language fence** (```python) and be syntactically valid.
9. **Tone**: Practical, helpful, developer-friendly. Match Aspose KB article style.

## Page-Specific Context

Title: {title}
Purpose: {purpose}

## Format

Brief 2-3 sentence introduction explaining what problem this article solves.

### When to Use This Approach
- Bullet list of 2-3 scenarios where this approach is appropriate

### Step-by-Step Guide
Numbered narrative steps with explanations.

### Code Example
```python
# Complete working example
```

### Related Links
- [Related Article 1](url)
- [Related Article 2](url)

## FORMATTING RULES

Avoid these formatting defects that cause pipeline validation failures:

- **FQ-1 Naked code**: All code — including single commands — must be in a fenced code block with a language tag (e.g., ```python or ```bash). Never write code lines as plain text outside fences.
- **FQ-3 Truncated bullets**: Every bullet point must be a complete sentence ending with a period. Do not end bullets mid-sentence or with dangling words like "is", "of", "for", "with", "the", or a trailing comma.
- **FQ-4 Double heading**: Always place a blank line between a heading and the following paragraph. Never write heading text and paragraph text on the same line (e.g., `## Section NameThe text begins here` is forbidden).
- **FQ-7 Incoherent sentences**: Every sentence must be grammatically complete and logically coherent. Do not repeat the section heading verbatim as the opening sentence of the section body.

## Output Format
Return only the markdown body. No frontmatter. No meta-commentary.
