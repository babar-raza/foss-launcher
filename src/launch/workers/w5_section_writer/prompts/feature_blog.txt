You are writing a feature highlight blog post for {product_name}.

Write an engaging, developer-friendly blog post that showcases ONE specific feature.

## Source Material

### Claims and Facts
{enriched_claims}

### Code Examples
{snippets}

### Cross-Section Links
{section_links}

## CRITICAL Rules

1. **Structure**: Introduction → Key Highlights → Quick Example → Next Steps.
2. **Friendly, approachable tone**: Use "you/your" voice. Be enthusiastic but not salesy.
3. **ONE quick code example** showing the feature in action. Keep it short (10-20 lines).
4. **Every factual statement MUST cite a claim** using [claim: CLAIM_ID] format. Only use claims from Source Material.
5. **NEVER dump raw parameter descriptions** or API signatures.
6. **NEVER redirect** with "see the documentation" or "refer to the API". Every section must be self-contained.
7. **Minimum 200 words**.
8. **Focus on WHY**: Explain why this feature matters, not just what it does.
9. When showing code, put the ENTIRE example in a SINGLE code fence. NEVER split one example across multiple fences separated by explanation text.
10. **Tone**: Friendly, enthusiastic, developer-focused. Match Aspose blog style.

## Page-Specific Context

Title: {title}
Purpose: {purpose}

## Format

Brief 2-3 sentence introduction hooking the reader with the problem this feature solves.

### Key Highlights
- **Highlight 1**: Why this matters to developers
- **Highlight 2**: How it saves time or effort
- **Highlight 3**: What makes it unique

### Quick Example
```python
# Short, compelling code example
```

### Next Steps
- Link to the product overview page (use the Products link from Cross-Section Links above).
- Link to detailed documentation (use the Docs link above).
- Link to API reference (use the Reference link above).
- Call to action (try it, contribute, etc.).

## FORMATTING RULES

Avoid these formatting defects that cause pipeline validation failures:

- **FQ-1 Naked code**: All code — including single commands — must be in a fenced code block with a language tag (e.g., ```python or ```bash). Never write code lines as plain text outside fences.
- **FQ-3 Truncated bullets**: Every bullet point must be a complete sentence ending with a period. Do not end bullets mid-sentence or with dangling words like "is", "of", "for", "with", "the", or a trailing comma.
- **FQ-4 Double heading**: Always place a blank line between a heading and the following paragraph. Never write heading text and paragraph text on the same line (e.g., `## Section NameThe text begins here` is forbidden).
- **FQ-7 Incoherent sentences**: Every sentence must be grammatically complete and logically coherent. Do not repeat the section heading verbatim as the opening sentence of the section body.

## Output Format
Return only the markdown body. No frontmatter. No meta-commentary.
