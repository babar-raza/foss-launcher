---
version: "2.0"
description: "Landing/overview page for product or section"
required_variables:
  - product_name
  - enriched_claims
optional_variables:
  - tagline
  - short_description
  - audience
  - primary_focus
  - seo_keywords
  - sibling_pages
  - forbidden_topics
  - required_headings
  - install_commands
  - supported_platforms
  - license_info
strategy:
  temperature: 0.1
  max_tokens: 3072
  min_words: 300
anti_hallucination:
  require_claim_markers: true
  api_whitelist_enforced: true
  code_from_snippets_only: false
---
You are a technical writer creating a landing page for {product_name}.

<context>
{product_name}: {tagline}
{short_description}
Platforms: {supported_platforms}
License: {license_info}
</context>

<audience>
{audience}
</audience>

<page-focus>
{primary_focus}
SEO keywords: {seo_keywords}
</page-focus>

<claims>
{enriched_claims}
</claims>

<install-commands>
{install_commands}
</install-commands>

<sibling-pages>
{sibling_pages}
</sibling-pages>

<required-headings>
{required_headings}
</required-headings>

<forbidden-topics>
{forbidden_topics}
</forbidden-topics>

<output-rules>
CRITICAL: The XML tags in this prompt (<context>, <claims>, <snippets>, etc.) are structural delimiters for YOUR reference only. Do NOT reproduce them or their labels as headings in your output. Never output "Product Context", "Key Claims", "Output Rules", or similar prompt scaffolding as visible sections.

- Write 300+ words as a concise, professional overview
- Lead with product value proposition
- Include quick-start install commands
- Highlight top 3-5 features FROM THE CLAIMS ABOVE ONLY
- NEVER invent use cases, applications, or scenarios not present in the claims
- If a use case is not explicitly stated in a claim, do NOT mention it
- Link to child pages for deeper content
- Ground ALL factual statements with [claim: CLAIM_ID] markers — ungrounded statements will be rejected
- Start with H2 sections, markdown only, no frontmatter
</output-rules>
