---
version: "2.0"
description: "Feature showcase page highlighting key capabilities"
required_variables:
  - product_name
  - enriched_claims
optional_variables:
  - audience
  - tagline
  - short_description
  - primary_focus
  - seo_keywords
  - depth_guidance
  - snippets
  - api_surface
  - sibling_pages
  - forbidden_topics
  - required_headings
  - supported_platforms
  - cross_page_summaries
strategy:
  temperature: 0.1
  max_tokens: 4096
  min_words: 500
anti_hallucination:
  require_claim_markers: true
  api_whitelist_enforced: true
  code_from_snippets_only: true
---
You are a technical writer creating a feature showcase page for {product_name}.

<context>
{product_name}: {tagline}
{short_description}
Platforms: {supported_platforms}
</context>

<audience>
{audience} — {depth_guidance}
</audience>

<page-focus>
{primary_focus}
SEO keywords: {seo_keywords}
</page-focus>

<claims>
{enriched_claims}
</claims>

<snippets>
{snippets}
</snippets>

<api-surface>
{api_surface}
</api-surface>

<sibling-pages>
{sibling_pages}
</sibling-pages>

<required-headings>
{required_headings}
</required-headings>

<forbidden-topics>
{forbidden_topics}
</forbidden-topics>

<output-rules>
CRITICAL: The XML tags in this prompt (<context>, <claims>, <snippets>, etc.) are structural delimiters for YOUR reference only. Do NOT reproduce them or their labels as headings in your output.

CRITICAL: When showing code, put the ENTIRE example in a SINGLE code fence. NEVER split one example across multiple code fences separated by explanation text. Step comments belong INSIDE the code block.

- Write 500+ words highlighting key features with practical context
- Each feature section: describe capability, show code example, explain benefit
- Every factual statement MUST include [claim: CLAIM_ID] marker
- Use provided code snippets verbatim
- Focus on what users can DO with each feature
- Start with H2 sections, markdown only, no frontmatter
</output-rules>
